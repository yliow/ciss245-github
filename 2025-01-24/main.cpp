#include <iostream>

// inline
// int max(int x, int y)
// {
//     return (x >= y ? x : y);
// }

int main()
{
    // std::cout << max(2, 3) << '\n';
    std::cout << (2 >= 3 ? 2 : 3) << '\n';
    
    return 0;
}
